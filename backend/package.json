{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "lint": "node --check src/*.js src/**/*.js",
    "test": "vitest run",
    "build": "echo \"No backend build step\""
  },
  "dependencies": {
    "@smart-document-pipeline/shared": "*",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.5"
  }
}
